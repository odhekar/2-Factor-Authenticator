<common:BootStrapper x:Class="TFAmvvm.App"
                     xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                     xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:common="using:Template10.Common"
                     xmlns:local="using:TFAmvvm">
    <common:BootStrapper.Resources>
        <Style x:Key="AppTitleTextBox" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
        </Style>
        <Style x:Key="PageTitleTextBox" TargetType="TextBlock">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="Foreground" Value="{ThemeResource SystemControlForegroundAccentBrush}" />
        </Style>
        <Style x:Key="ButtonHintTemplate" TargetType="ContentControl">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ContentControl">
                        <Grid Height="Auto" Width="Auto">
                            <Path Data="M 0 0 L 160 0 L 160 30 L 84 30 L 80 38 L 76 30 L 0 30 Z" Fill="{ThemeResource SystemControlForegroundAccentBrush}" />
                            <ContentPresenter Margin="4"
                                              HorizontalAlignment="Center" />
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </common:BootStrapper.Resources>
</common:BootStrapper>
